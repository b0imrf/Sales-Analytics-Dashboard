<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_sale_order_form_inherit_branch" model="ir.ui.view">
        <field name="name">sale.order.form.inherit.branch</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <field name="company_id" position="after">
                <field name="branch_id"/>
            </field>
        </field>
    </record>

    <record id="view_sale_order_tree_inherit_branch" model="ir.ui.view">
        <field name="name">sale.order.tree.inherit.branch</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_tree"/>
        <field name="arch" type="xml">
            <field name="company_id" position="after">
                <field name="branch_id" optional="show"/>
            </field>
        </field>
    </record>
</odoo>
